/**
\page misc Misc

\subsection miscintro Introduction

The misc library contains a set of usefull classes :
- The stream system allows a programmer to serialize in/out objects from any class
from/to any input/output device. See NLMISC::IStream.
- The math classes handle 3D vectors, matrices and so on.
- The config_file classes allow to load and parse configuration files.
- The library also has debug and basic display facilities, support for RTTI
and smart pointers.

 */
