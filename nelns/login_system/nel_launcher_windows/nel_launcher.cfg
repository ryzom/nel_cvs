
// host where to find the login server
StartupHost = "localhost";

// the html access
StartupPage = "/index.php";

// first is the application name, second is the path+exe name the first is the base path
Application = { "snowballs", "../../../snowballs2/client/Debug/snowballs.exe", "../../../snowballs2/client" };
